{
  "name": "easyway-lms-root",
  "private": true,
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "next": "16.2.9",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "scripts": {
    "vercel-build": "npm --prefix prototype install --include=dev --no-audit --no-fund && node scripts/vercel-migrate.mjs && npm --prefix prototype run build && node scripts/vercel-postbuild.mjs",
    "build": "npm run vercel-build",
    "seed:spaces": "tsx --tsconfig tsconfig.json scripts/seed-community-spaces.ts",
    "verify:community": "tsx --tsconfig tsconfig.json scripts/verify-community.ts"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
